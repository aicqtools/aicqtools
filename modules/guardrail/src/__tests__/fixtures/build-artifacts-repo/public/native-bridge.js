// Hand-written Capacitor bridge file. Lives under public/ but is real code that should be
// scanned by default — only the narrow `public/_next /static /build` subdirs are excluded.
console.log('bridge ready');
