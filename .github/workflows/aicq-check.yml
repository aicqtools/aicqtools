name: AICQ check (self-dogfood)

# Run aicq against this very monorepo on every PR / push to main.
# This is the canonical dogfooding workflow — every change must pass aicq's own rules.

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm build

      - name: Run unit tests
        run: pnpm test

      - name: Regression — large file parsing (32KB+ tsx/ts/py)
        run: |
          node e2e/large-file-fixture/generate.mjs
          node e2e/large-file-fixture/verify.mjs
        # Hard gate — this regression must never come back. No continue-on-error.

      - name: Regression — install isolation (single tree-sitter native copy)
        run: |
          mkdir -p /tmp/aicq-tarballs
          pnpm -r pack --pack-destination /tmp/aicq-tarballs
          node e2e/install-isolation/run-bisect.mjs /tmp/aicq-tarballs
        # Hard gate — alpha.4's multi-instance regression must never come back.

      - name: Run aicq check on talkup-mirror
        working-directory: examples/talkup-mirror
        run: node ../../packages/cli/dist/bin.js check --locale ko --no-cache
        # talkup-mirror is intentionally seeded with violations,
        # so this step is allowed to fail without breaking the workflow.
        continue-on-error: true

      - name: Run aicq benchmark
        run: |
          node e2e/benchmark/generate.mjs
          node e2e/benchmark/run.mjs
